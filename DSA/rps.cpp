#include <iostream>
using namespace std;
enum Choice (ROCK,PAPER,SCISSORS);
enum Winner (PLAYER1,PLAYER2,TIE);
int main()
{
    int n;
    Choice choice1,choice2;
    Winner winner;
    cout<<"Choose ROCK(0),Paper(1),or "
    return 0;
}